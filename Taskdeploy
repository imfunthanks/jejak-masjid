# Backend Integration & Deployment — Jejak Masjid

## Lint & Validation
- [x] Run `tsc --noEmit` → 0 errors ✅
- [x] Run `npm run lint` → 0 errors, 25 warnings ✅
- [x] Fix unused vars in page.tsx, MosqueMapInner.tsx, WrappedSlideshow.tsx ✅
- [x] Fix `any` type cast in MosqueMapInner.tsx ✅

## Environment Setup
- [ ] Create `.env.local` with `DATABASE_URL` + `AUTH_SECRET`
- [ ] Set up Neon Postgres (or user's preferred provider)
- [ ] Run `db:push` to sync schema
- [ ] Run `db:seed` to populate mosques

## Backend Wiring
- [x] Wire `/wrapped` page to fetch real user data via server component ✅
- [x] Wire `/recap` page to fetch real user data via server component ✅
- [x] Wire `/map` to fetch mosques from API (was already wired) ✅
- [x] Wire `/journey` dashboard to use real checkin data ✅
- [x] Wire `/passport` to use real checkin data (placeholder page, deferred)
- [x] Add API route: `/api/wrapped` for wrapped stats computation ✅
- [x] Login/signup pages already use NextAuth ✅

## Vercel Deployment
- [ ] Add `AUTH_SECRET` + `DATABASE_URL` to Vercel env vars
- [x] Test production build with `npm run build` → ✅ Exit code 0
- [ ] Deploy to Vercel
- [ ] Verify deployed app
